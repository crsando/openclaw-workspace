# 2026-05-01

- 按达叔要求通过 LobeHub Skills Marketplace 注册并安装了 `openclaw-skills-memos` 到 `/home/ralmia/.openclaw/skills/openclaw-skills-memos`。
- 已读取安装后的 SKILL.md；该技能要求环境变量 `MEMOS_URL` 和 `MEMOS_TOKEN` 才能实际调用 Memos API。
- 当前运行环境中未检测到 `MEMOS_URL` / `MEMOS_TOKEN`。
- 达叔随后提供了 `MEMOS_TOKEN`，已仅用于本次临时验证，没有写入记忆文件。
- 验证时发现安装的 `openclaw-skills-memos` 自带两个问题：
  1. `list_memos()` 调用 `_request()` 时参数位置写错，导致查询参数未正确传递；
  2. 直接拼接 `MEMOS_URL + endpoint`，当 URL 自带尾斜杠时会产生 `//api/...`，服务端返回 HTML，JSON 解析失败。
- 已在安装目录本地修复上述两个问题，现已成功 `list` 和 `create`。
- 成功创建测试 memo：`memos/65`，uid `9u9wqStSJUJc6Ghfb7qPxY`。
