# 2026-05-01

- 按达叔要求通过 LobeHub Skills Marketplace 注册并安装了 `openclaw-skills-memos` 到 `/home/ralmia/.openclaw/skills/openclaw-skills-memos`。
- 已读取安装后的 SKILL.md；该技能要求环境变量 `MEMOS_URL` 和 `MEMOS_TOKEN` 才能实际调用 Memos API。
- 当前运行环境中未检测到 `MEMOS_URL` / `MEMOS_TOKEN`。
- 达叔随后提供了 `MEMOS_TOKEN`，已仅用于本次临时验证，没有写入记忆文件。
- 验证时发现安装的 `openclaw-skills-memos` 自带两个问题：
  1. `list_memos()` 调用 `_request()` 时参数位置写错，导致查询参数未正确传递；
  2. 直接拼接 `MEMOS_URL + endpoint`，当 URL 自带尾斜杠时会产生 `//api/...`，服务端返回 HTML，JSON 解析失败。
- 已在安装目录本地修复上述两个问题，现已成功 `list` 和 `create`。
- 成功创建测试 memo：`memos/65`，uid `9u9wqStSJUJc6Ghfb7qPxY`。
- 已通过 LobeHub Marketplace 为 OpenClaw 注册客户端凭据，保存位置：`/home/ralmia/.lobehub-market/credentials.json`。
- 已通过 LobeHub 安装 `openclaw-skills-memos` 到 `/home/ralmia/.openclaw/skills/openclaw-skills-memos`。
- `openclaw-skills-memos` 本地修复点：`MEMOS_URL` 启动时去尾斜杠；`list_memos()` 改为正确传递 `params`。否则会因 `//api/...` 或参数位置错误导致返回 HTML/解析失败。
- Memos 连通性已验证，成功创建测试 memo：`memos/65`（uid `9u9wqStSJUJc6Ghfb7qPxY`）。
- 已使用本地 IMA skill（`/home/ralmia/.openclaw/workspace/ima-skills-1.1.7/ima-skill/ima_api.cjs`）成功创建一篇整理 Memos 技能内容的 IMA 笔记，note_id：`7455830693663761`。
- 本机已存在可用 IMA OpenAPI 凭证文件：`~/.config/ima/client_id` 与 `~/.config/ima/api_key`。
- 新建的 IMA 笔记内容涵盖：Memos skill 安装来源、环境变量要求、验证过程、两个 bug 及本地修复、测试 memo 结果与后续建议。
- 达叔要求：以后所有交易记录默认用 Memos 记录，并统一补充标签 #trade。
- 达叔要求：以后发来的餐厅信息，默认补充写入 IMA 笔记《想去的餐厅》（note_id: 7454524851634077），只追加整理，不删除原内容。
- 已验证 Memos 实例可用：地址为 http://192.168.5.10:5230/，用户提供的 token 能成功读取 API。
- 已定位并修复 openclaw-skills-memos 的 list bug：/home/ralmia/.openclaw/skills/openclaw-skills-memos/memos.py 中 list_memos 调用改为显式传 data=None, params=params。
- 达叔要求：交易记录默认用 Memos 记录，并统一补充标签 #trade。
- 达叔要求：以后发来的餐厅信息，默认补充写入 IMA 笔记《想去的餐厅》（note_id: 7454524851634077），只追加整理，不删除原内容。
- 已查询 2026年4月 交易相关 memos：共 2 条，分别是中国核电买入计划、生猪2607买入记录。
